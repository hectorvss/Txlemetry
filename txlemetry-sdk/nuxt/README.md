# @txlemetry/nuxt

Txlemetry module for Nuxt.

```bash
npm install @txlemetry/nuxt
```
```ts
// nuxt.config.ts
export default defineNuxtConfig({ modules: ['@txlemetry/nuxt'] })
```
Re-export of @posthog/nuxt. Validate in a real Nuxt project before relying on it (module resolution).
