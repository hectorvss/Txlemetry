// @txlemetry/js — Txlemetry browser SDK.
// Thin, behavior-preserving re-export of the analytics engine, so
//   import txlemetry from '@txlemetry/js'
// behaves exactly like the default instance. No runtime change.
export { default } from 'posthog-js'
export * from 'posthog-js'
